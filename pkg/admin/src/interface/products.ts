export interface IProducts {
    price: string;
    description: string;
    name: string;
    available: string;
    supplierID: string;
    categoryID: string;
}

export interface IBaseResponse {
    msg: string;
}